# The project is designed to learning soft system for me.

# git command
# git init
# git add <filename>
# git commit -m "comments"
# 添加仓库origin
# git remote add origin git@github.com:wuyan19/neos.git
# git push -u origin master
#
# git fetch origin
# git merge origin/master
# git push origin master
